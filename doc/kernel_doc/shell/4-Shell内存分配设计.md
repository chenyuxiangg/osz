## 内存管理设计

Shell在资源受限环境下采用**精细内存控制**：

### 静态内存
- **固定缓冲区**：1024字节输入缓冲区，编译期确定大小
- **位域优化**：使用位域压缩状态变量，减少内存占用
- **结构体对齐**：合理排列结构体成员，避免内存空洞

### 动态内存
- **按需分配**：历史命令、参数数组、临时缓冲区动态分配
- **及时释放**：命令执行后立即释放参数内存，避免泄漏
- **错误回滚**：分配失败时清理已分配资源，保持系统稳定